# CodeCard — 当前进度

> 更新于 2026-05-21

## 项目状态

| 项 | 状态 |
|----|:--:|
| APP 名称 | CodeCard |
| TypeScript | 零错误 |
| Metro Bundle | 1305 模块，零错误 |
| Expo SDK | 55.0.31 |
| Reanimated | 4.1.7 + react-native-worklets 0.8.0 |
| newArchEnabled | false |

## P0 已修复（架构重构 + Bug 修复）

### 架构：注册表模式
- [x] **动画注册表** `src/data/animations/index.ts` — animationId → {scenario, loadComponent}，新增动画只需 +1 行
- [x] **卡片渲染器** `src/components/cards/renderCard.tsx` — 从 NodeScreen 抽出，统一派发
- [x] **Store 按学科隔离** `courses: Record<string, CourseProgress>` — 多学科互不污染
- [x] **手动持久化** `subscribe → AsyncStorage` — 避免 zustand persist 与 React Native Fabric 兼容问题

### Bug 修复
- [x] QuizScreen 调 completeCard — 测验完成后正确记录
- [x] XP 升级 while 循环 — 大额 XP 正确升多级
- [x] 快速连点不重复加 XP — completeCard 返回 boolean 防重
- [x] 填空题大小写不敏感 — normalize()
- [x] 卡片加 key — renderCard 内统一 key={card.id}
- [x] 卡片位置记忆 — store.nodePositions 持久化，退出再进恢复位置
- [x] 上一张按钮 — footer 左侧 ← 上一张
- [x] 完成页 — done 后显示 🎉 学习完成 + 返回课程
- [x] PracticeCard 最后一张按钮改为"完成"
- [x] MemoryBox 地址列与网格对齐
- [x] AGENTS.md 版本 v54 → v55

### 数据流架构
```
新学科 → data/courses/index.ts +1 行        （不用改源码）
新章节/节点/卡片 → 纯数据                     （不用改源码）
新卡片类型 → types + renderCard +1 case + 组件 （2 文件）
新动画 → data/animations/index.ts +1 行 + 场景 + 组件 （1 文件）
新题型 → 待 P1
```

## 待 P1

| # | 问题 | 位置 |
|---|------|------|
| 1 | PracticeCard 与 QuizScreen 题型逻辑重复 | 抽取 questionHandlers.ts |
| 2 | CodeCard 忽略 highlights 行高亮 | CodeCard.tsx |
| 3 | 清理死代码：checkLevelUp、streak/badges、AnimationSandbox | 全局 |
| 4 | as any 类型安全 | renderCard / NodeScreen |
| 5 | 颜色主题统一 | 全局硬编码 #4a9eff 等 |

## 内容完成度

```
C++ 43 个计划模块
  ├── 01 起步：    ✅ 2/2 模块（5 张卡片）
  ├── 02 进阶：    ❌ 0/10
  ├── 03 OOP：     ❌ 0/6
  ├── 04 STL：     ❌ 0/6
  ├── 05 泛型：    ❌ 0/4
  └── 06 现代C++： ❌ 0/9
────────────────────────────
整体：2/43 = 4.6%
```
