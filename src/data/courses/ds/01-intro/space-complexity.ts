import type { PathNode } from '@/types';

export const spaceComplexityNode: PathNode = {
  id: 'ds-intro-space-complexity',
  courseId: 'ds',
  type: 'knowledge',
  moduleId: 'intro',
  module: '绪论',
  title: '空间复杂度与时空权衡',
  cards: [
    {
      id: 'ds-intro-space-complexity-c1',
      cardType: 'concept',
      content: {
        title: '空间复杂度是什么',
        body: [
          '算法不光花时间，还占内存。',
          '空间复杂度 = 算法运行中额外占用的内存（不算输入数据本身）。',
          '',
          '例 1 — 原地操作，O(1) 额外空间：',
          'int findMax(int arr[], int n) {',
          '    int maxVal = arr[0];           // 只用了一个变量',
          '    for (int i = 1; i < n; i++)',
          '        if (arr[i] > maxVal) maxVal = arr[i];',
          '    return maxVal;',
          '}',
          '不管 n 多大，只需要 1 个临时变量 → O(1)。',
          '',
          '例 2 — 新建数组，O(n) 额外空间：',
          'int* copyArray(int arr[], int n) {',
          '    int* newArr = new int[n];      // new 了 n 个 int',
          '    for (int i = 0; i < n; i++)',
          '        newArr[i] = arr[i];',
          '    return newArr;',
          '}',
          '额外空间随 n 增长 → O(n)。',
          '',
          '例 3 — 递归栈也算空间！',
          'int factorial(int n) {',
          '    if (n <= 1) return 1;',
          '    return n * factorial(n - 1);   // 每次递归压一层栈',
          '}',
          '递归深度 n → 调用栈 O(n) 空间。',
          '',
          '[408 考点] 递归函数的空间复杂度看调用栈深度，',
          '不是看代码里有没有 new。',
        ].join('\n'),
      },
    },
    {
      id: 'ds-intro-space-complexity-c2',
      cardType: 'concept',
      content: {
        title: '空间换时间 vs 时间换空间',
        body: [
          '时间和空间像跷跷板——省内存往往要多花时间，省时间往往要多占内存。',
          '',
          '空间换时间的典型：哈希表',
          '  不用哈希表：每次查找遍历 O(n) 时间，O(1) 空间',
          '  用哈希表：建表占 O(n) 空间，每次查找 O(1) 时间',
          '  多占内存，省了时间 → 这就是空间换时间',
          '',
          '时间换空间的典型：嵌入式设备',
          '  单片机内存可能只有 256KB。',
          '  策略：紧凑 bit 位存储、数据实时计算不缓存、',
          '  用链表不用数组（虽然慢但不需连续大块内存）。',
          '',
          '工程上的选择原则：',
          '  服务器后端 → 偏向空间换时间（内存便宜，体验是命）',
          '  手机 App   → 折中（内存有限，但也不能卡）',
          '  单片机/IoT → 偏向时间换空间（电池和内存最紧张）',
          '',
          '没有最优解，只有最适合的方案。',
          '这就是为什么学数据结构——了解每种结构的代价，才能正确选择。',
        ].join('\n'),
      },
    },
    {
      id: 'ds-intro-space-complexity-c3',
      cardType: 'concept',
      content: {
        title: '实战二选一：你来判断',
        body: [
          '场景 1：短视频 App 的点赞列表',
          '需要存"谁赞了"。方案 A：每次点赞追加到数组，遍历查 O(n)。',
          '方案 B：哈希表以用户 ID 为 key，O(1) 查但多占内存。',
          '',
          '→ 正确答案是折中：小数据量时用数组，量大时升级哈希表。',
          '手机内存有限，但 O(n) 遍历百万数量级不可接受。',
          '',
          '场景 2：气象站数据采集',
          '野外气象站每小时采一次温度，存够一年（8760 条）。',
          '内存极少，不需实时查询——每月传回一次就行。',
          '方案 A：数组全存。方案 B：内存只存 24 条，旧数据写闪存。',
          '',
          '→ B 更合适：内存有限，不需要随时查历史数据。',
          '',
          '关键教训：选方案之前，先问"数据量多大？瓶颈在哪？"',
        ].join('\n'),
      },
    },
    {
      id: 'ds-intro-space-complexity-c4',
      cardType: 'concept',
      content: {
        title: '常见数据结构的时空代价速查',
        body: [
          '一张表，预览后面要学的所有结构。现在不用背，学完一个回来验证一个。',
          '',
          '数组：',
          '  按位置访问 O(1)  |  按值查找 O(n)',
          '  插入 O(n)  |  删除 O(n)  |  额外空间 O(1)',
          '',
          '链表：',
          '  按位置访问 O(n)  |  按值查找 O(n)',
          '  插入 O(1)* |  删除 O(1)* |  额外空间 O(1)',
          '  *找到位置后是 O(1)，但找位置本身要 O(n)',
          '',
          '栈 / 队列：',
          '  访问 —  |  查找 O(n)',
          '  插入 O(1)  |  删除 O(1)  |  额外空间 O(1)',
          '',
          '二叉排序树（平衡）：',
          '  访问 —  |  查找 O(log n)**',
          '  插入 O(log n)  |  删除 O(log n)  |  额外空间 O(n)',
          '  **不平衡时会退化到 O(n)',
          '',
          '哈希表：',
          '  访问 —  |  查找 O(1)***',
          '  插入 O(1)  |  删除 O(1)  |  额外空间 O(n)',
          '  ***冲突严重时退化到 O(n)',
          '',
          '这张表就是整门课的"代价速查手册"。',
        ].join('\n'),
      },
    },
    {
      id: 'ds-intro-space-complexity-c5',
      cardType: 'practice',
      content: {
        question: [
          '以下三种找数组重复元素的写法，各自时空复杂度是什么？',
          '',
          '写法 A：双重循环暴力比较',
          '写法 B：先排序再检查相邻元素',
          '写法 C：用 unordered_set 记录已出现的值',
          '',
          '如果要检查 1000 万条用户 ID 是否有重复，选哪个？为什么？',
        ].join('\n'),
        questionType: 'choice',
        options: [
          '选 C，因为 O(n) 时间 + O(n) 空间 用空间换时间，对 1000 万数据最合理',
          '选 A，因为 O(n²)/O(1) 最简单',
          '选 B，因为 O(n log n)/O(1) 平衡',
          '选哪个都一样',
        ],
        answer: '选 C，因为 O(n) 时间 + O(n) 空间 用空间换时间，对 1000 万数据最合理',
        explanation: [
          '三种写法的时空复杂度：',
          'A：双重循环 → O(n²) 时间，O(1) 空间。',
          'B：先排序再检查 → O(n log n) 时间，O(1) 空间。',
          'C：unordered_set → O(n) 时间，O(n) 空间。',
          '',
          '1000 万数据：O(n²) ≈ 100 万亿次比较，等不起。',
          'O(n log n) ≈ 2.3 亿次，可以接受但偏慢。',
          'O(n) 时间 + O(n) 空间 ≈ 1000 万次操作 + 几十 MB 内存。',
          '对服务端来说，几十 MB 换秒级响应是值得的。',
          '但如果在单片机上——32KB RAM——就不能用 C 方案了。',
        ].join('\n'),
      },
    },
    {
      id: 'ds-intro-space-complexity-c6',
      cardType: 'concept',
      content: {
        title: '模块总结地图',
        body: [
          '第一章学完了。回顾一下：',
          '',
          '概念篇（01-01）：',
          '  ├── 逻辑结构四种：集合 / 线性 / 树形 / 图状',
          '  ├── 存储结构四种：顺序 / 链式 / 索引 / 散列',
          '  ├── 三要素：逻辑 + 存储 + 运算',
          '  └── 算法五特性 + 程序 = 数据结构 + 算法',
          '',
          '技能篇（01-02）：',
          '  ├── 看到代码能算 O(几)',
          '  ├── 加法规则 & 乘法规则',
          '  ├── 嵌套循环 → O(n²) / while 折半 → O(log n)',
          '  ├── 最好 / 最坏 / 平均 → 大 O 默认最坏',
          '  └── 递归复杂度：递归树 + 记住常用结论',
          '',
          '工程篇（01-03）：',
          '  ├── 空间复杂度（递归栈也算）',
          '  ├── 空间换时间 vs 时间换空间',
          '  ├── 每种结构有不同的时空代价',
          '  └── 怎么选择：先问数据量，再问瓶颈在哪',
          '',
          '下一模块预告：线性表。亲手写顺序表和链表，',
          '验证第一章学到的所有复杂度知识。',
        ].join('\n'),
      },
    },
  ],
};
